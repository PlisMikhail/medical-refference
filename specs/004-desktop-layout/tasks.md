# Tasks: Раскладка для широкого экрана

**Input**: [spec.md](./spec.md), [plan.md](./plan.md), [research.md](./research.md),
[quickstart.md](./quickstart.md)

**Tests**: тест раскладки включён — он проверяет то, чего не видит ни одна
другая проверка: что в DOM ровно один вариант навигации и что дефолт мобильный.

**Организация**: по пользовательским историям. US3 (дефект ленты) вынесена
вперёд и уже закрыта — она не зависит от раскладки и чинит недоступность.

---

## Phase 1: User Story 3 — лента чипов работает мышью (Priority: P1) ✅ сделано до фичи

- [x] T001 [US3] Вернуть полосу прокрутки ленты для точных указателей в `src/components/ProtocolSectionNav.vue`, оставив её скрытой для грубых
- [x] T002 [US3] Добавить обработчик колеса, переводящий вертикальную дельту в горизонтальную прокрутку ленты; перехватывать событие только если лента сдвинулась

---

## Phase 2: Foundational — определение раскладки

**⚠️ БЛОКИРУЕТ US1: без него нечем выбирать представление.**

- [x] T003 Создать `src/composables/useMediaQuery.ts`: подписка на медиазапрос через `matchMedia`, реактивное значение, отписка при размонтировании, дефолт `false` при отсутствии `matchMedia` (R3)
- [x] T004 Экспортировать из него именованный признак широкого экрана с порогом 1024 px в одном месте (R1), чтобы три потребителя не разъехались

---

## Phase 3: User Story 1 — все секции видны сразу (Priority: P1) 🎯 MVP

**Цель**: на широком экране оглавление сбоку, переход одним кликом.

**Independent Test**: открыть памятку по ВСТЭ в окне 1280 px, увидеть все
тринадцать пунктов, кликнуть в последний.

- [x] T005 [US1] В `src/components/ProtocolSectionNav.vue` добавить второе представление — вертикальное оглавление, рендерящееся вместо ленты на широком экране; в DOM одновременно только одно (FR-003)
- [x] T006 [US1] Переиспользовать для оглавления существующие переход по клику, подсветку активной секции и цвета типов секций — без ветвления логики (FR-007…FR-009)
- [x] T007 [US1] Закрепить оглавление при прокрутке (FR-004), не регистрируя его в слоте закреплённой обвязки (R4, FR-010)
- [x] T008 [US1] В `src/views/ProtocolView.vue` развести на широком экране оглавление и содержимое в две колонки; на узком — прежний одноколоночный порядок
- [x] T009 [US1] В `src/App.vue` расширить контейнер каркаса на широком экране, оставив колонку текста ограниченной (FR-005, R5)

---

## Phase 4: User Story 2 — на телефоне ничего не изменилось (Priority: P1)

**Цель**: отсутствие регрессии в единственном сценарии цейтнота.

**Independent Test**: прогнать сценарии навигации фичи 001 на ширине ~380 px.

- [x] T010 [US2] Убедиться, что `tests/section-nav.spec.ts` проходит **без единой правки**; если он потребовал правки — это регрессия, а не устаревший тест
- [x] T011 [US2] Проверить вручную на ширине ~380 px: тап по чипу, подтягивание уехавшего чипа, подсветка активной секции при прокрутке, отступ якоря

---

## Phase 5: User Story 4 — текст остаётся читаемым (Priority: P2)

- [x] T012 [US4] Проверить на ширине 1920 px, что строка текста протокола не растягивается во всю ширину, а свободное место ушло в оглавление и поля

---

## Phase 6: Polish

- [x] T013 [P] Добавить `tests/desktop-layout.spec.ts`: при широком экране рендерится оглавление и НЕ рендерится лента, при узком — наоборот, при отсутствии `matchMedia` — лента; в обоих случаях число пунктов равно числу секций
- [x] T014 [P] Обновить `README.md`: раскладка для широкого экрана и порог переключения
- [x] T015 Прогнать полный DoD из [quickstart.md](./quickstart.md), включая `validate:data` и `verify:transcription` — они не должны были затронуться
- [x] T016 Закоммитить фичу; `src/data/` не должен быть в диффе

---

## Зависимости

- **Phase 2 → Phase 3.** Без определения раскладки выбирать нечего.
- **Phase 4** проверяет результат Phase 3 и идёт следом.
- **Phase 5, 6** независимы между собой.
- **Phase 1** уже закрыта и ни от чего не зависит.

## MVP

Phases 2–3: оглавление сбоку на широком экране. Phase 4 — обязательная страховка,
а не улучшение: без неё фичу нельзя выпускать.

---

## Что осталось владельцу

- **Прокрутка ленты колесом и полосой** — проверяется мышью, в headless это
  не имитируется честно.
- **Офлайн на прод-сборке** — в браузере.

Всё остальное проверено, включая замеры геометрии в headless-браузере:
раскладки на 1440×900 и 390×844, тринадцать пунктов оглавления на первом
экране, отступ якоря в обеих раскладках и в обе стороны прокрутки.
